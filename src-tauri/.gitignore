# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Sidecar empaquetado (backend/cuadernofp-backend.spec, Fase 2 del plan) --
# binario generado, no fuente; además supera con creces el límite de 100MB
# de GitHub. Se reconstruye con PyInstaller antes de cada `tauri build`.
/binaries/
